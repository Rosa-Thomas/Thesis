import { ChainClient } from "drand-client";
import { Stanza } from "../age/age-encrypt-decrypt";
export declare function createTimelockEncrypter(client: ChainClient, roundNumber: number): (fileKey: Uint8Array) => Promise<Array<Stanza>>;
export declare function hashedRoundNumber(round: number): Uint8Array;
