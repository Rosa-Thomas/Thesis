import { Stanza } from "./age-encrypt-decrypt";
declare class NoOpEncDec {
    static wrap(filekey: Uint8Array): Promise<Array<Stanza>>;
    static unwrap(recipients: Array<Stanza>): Promise<Uint8Array>;
}
export { NoOpEncDec };
