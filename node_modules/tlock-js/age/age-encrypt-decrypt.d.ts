/// <reference types="node" />
import { Buffer } from "buffer";
type FileKey = Uint8Array;
type EncryptionWrapper = (fileKey: FileKey) => Promise<Array<Stanza>>;
type DecryptionWrapper = (recipients: Array<Stanza>) => Promise<FileKey>;
export type Stanza = {
    type: string;
    args: Array<string>;
    body: Uint8Array;
};
export declare function encryptAge(plaintext: Uint8Array, wrapFileKey?: EncryptionWrapper): Promise<string>;
export declare function decryptAge(payload: string, unwrapFileKey?: DecryptionWrapper): Promise<Buffer>;
export {};
