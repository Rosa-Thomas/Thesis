type Stanza = {
    type: string;
    args: Array<string>;
    body: Uint8Array;
};
type AgeEncryptionInput = {
    fileKey: Uint8Array;
    version: string;
    recipients: Array<Stanza>;
    body: Uint8Array;
    headerMacMessage: string;
};
type AgeEncryptionOutput = {
    header: {
        version: string;
        recipients: Array<Stanza>;
        mac: Uint8Array;
    };
    body: Uint8Array;
};
export declare function writeAge(input: AgeEncryptionInput): string;
export declare function header(input: AgeEncryptionInput): string;
export declare function readAge(input: string): AgeEncryptionOutput;
export {};
