type ui8a = Uint8Array;
export declare class STREAM {
    static seal(plaintext: ui8a, privateKey: ui8a): Uint8Array;
    static open(ciphertext: ui8a, privateKey: ui8a): Uint8Array;
    key: ui8a;
    nonce: ui8a;
    nonceView: DataView;
    counter: number;
    constructor(key: ui8a);
    encryptChunk(chunk: ui8a, isLast: boolean, output: ui8a): void;
    decryptChunk(chunk: ui8a, isLast: boolean, output: ui8a): void;
    incrementCounter(): void;
    clear(): void;
}
export {};
